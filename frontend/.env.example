# ForecastGuard Frontend Environment Variables
# In development with Vite proxy or production behind Nginx reverse proxy, leave as /api
# If frontend is deployed separately from backend, set to full API URL (e.g. http://localhost:8000/api)
VITE_API_URL=/api
